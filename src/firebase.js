// Firebase has been removed for demo purposes
// This file is kept for compatibility but all Firebase imports have been replaced with local storage

// If you need to reconnect Firebase, restore the original configuration here
// and update the imports in AuthContext.jsx and AttendanceContext.jsx

console.log('Firebase disabled - using local storage for demo mode');

export const firebaseConfig = null;
export const auth = null;
export const db = null;
export const storage = null;
